name: Cleaning Start SMS Cost Optimization Apply

on:
  push:
    branches:
      - agent/cleaning-start-sms-cost-optimization-v1
    paths:
      - ".github/workflows/cleaning-start-sms-cost-optimization-apply.yml"

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/cleaning-start-sms-cost-optimization-v1
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Apply cleaning start SMS optimization
        run: node scripts/apply-cleaning-start-sms-cost-optimization.mjs
      - run: npm ci
      - name: Cleaning start SMS segment tests
        run: npx tsx --test src/services/cleaning-start-sms-cost-optimization.test.ts
      - name: Bundle messaging service
        run: npx esbuild src/services/messaging.service.ts --bundle --platform=node --packages=external --outfile=/tmp/messaging.service.js
      - name: Commit validated messaging change only
        run: |
          git config user.name "freddiefernandezmaysonet-netizen"
          git config user.email "freddiefernandezmaysonet@gmail.com"
          git add src/services/messaging.service.ts
          git diff --cached --check
          git commit -m "Optimize cleaning start SMS segment usage"
          git push origin HEAD:agent/cleaning-start-sms-cost-optimization-v1
