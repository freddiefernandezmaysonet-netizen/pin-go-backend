name: Apply Mission Control Readiness Policy

on:
  push:
    branches:
      - agent/mission-control-engine-readiness-v1-backend
    paths:
      - ".github/workflows/apply-mission-control-readiness-policy.yml"
      - ".github/patches/mission-control-readiness-policy.patch"

permissions:
  contents: write

jobs:
  apply-readiness-policy:
    name: Apply reviewed readiness policy
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout sprint branch
        uses: actions/checkout@v4
        with:
          ref: agent/mission-control-engine-readiness-v1-backend
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/pingo

      - name: Verify patch applies cleanly
        run: git apply --check .github/patches/mission-control-readiness-policy.patch

      - name: Apply reviewed patch
        run: git apply .github/patches/mission-control-readiness-policy.patch

      - name: Verify only the service changed
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t changed_files < <(git diff --name-only)

          if (( ${#changed_files[@]} != 1 )); then
            printf 'Unexpected changed files:\n%s\n' "${changed_files[*]}"
            exit 1
          fi

          if [[ "${changed_files[0]}" != "src/apms/mission-control.service.ts" ]]; then
            printf 'Unexpected changed file: %s\n' "${changed_files[0]}"
            exit 1
          fi

          git diff --check

      - name: Compile Mission Control service
        run: |
          npx --yes esbuild@0.28.1 src/apms/mission-control.service.ts \
            --bundle \
            --platform=node \
            --format=esm \
            --target=node22 \
            --packages=external \
            --log-level=warning \
            --outfile=/tmp/mission-control-service.mjs

      - name: Run Mission Control contract tests
        run: npx tsx --test src/apms/__tests__/mission-control-read-model.test.ts

      - name: Commit only the reviewed service change
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/apms/mission-control.service.ts
          git diff --cached --check
          git commit -m "fix: align mission control readiness semantics"
          git push origin HEAD:agent/mission-control-engine-readiness-v1-backend
