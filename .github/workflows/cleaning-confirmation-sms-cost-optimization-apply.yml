name: Cleaning Confirmation SMS Cost Optimization Apply

on:
  push:
    branches:
      - agent/cleaning-confirmation-sms-cost-optimization-v1
    paths:
      - ".github/workflows/cleaning-confirmation-sms-cost-optimization-apply.yml"

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/cleaning-confirmation-sms-cost-optimization-v1
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Apply cleaning confirmation SMS optimization
        run: node scripts/apply-cleaning-confirmation-sms-cost-optimization.mjs
      - run: npm ci
      - name: Cleaning confirmation SMS segment tests
        run: npx tsx --test src/services/cleaning-confirmation-sms-cost-optimization.test.ts
      - name: Bundle cleaning confirmation SMS builder
        run: npx esbuild src/services/cleaning-confirmation-sms-body.service.ts --bundle --platform=node --packages=external --outfile=/tmp/cleaning-confirmation-sms-body.service.js
      - name: Bundle cleaning confirmation dispatch service
        run: npx esbuild src/services/cleaning-confirmation-dispatch.service.ts --bundle --platform=node --packages=external --outfile=/tmp/cleaning-confirmation-dispatch.service.js
      - name: Commit validated service change only
        run: |
          git config user.name "freddiefernandezmaysonet-netizen"
          git config user.email "freddiefernandezmaysonet@gmail.com"
          git add src/services/cleaning-confirmation-dispatch.service.ts
          git diff --cached --check
          git commit -m "Optimize cleaning confirmation SMS segment usage"
          git push origin HEAD:agent/cleaning-confirmation-sms-cost-optimization-v1
