name: Cleaning Ready SMS Cost Optimization Validation

on:
  push:
    branches:
      - agent/cleaning-ready-sms-cost-optimization-v1
    paths:
      - ".github/workflows/cleaning-ready-sms-cost-optimization-validation.yml"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Cleaning ready SMS segment tests
        run: npx tsx --test src/services/cleaning-ready-sms-cost-optimization.test.ts
      - name: Bundle cleaning ready SMS builder
        run: npx esbuild src/services/cleaning-ready-sms-body.service.ts --bundle --platform=node --packages=external --outfile=/tmp/cleaning-ready-sms-body.service.js
      - name: Bundle cleaning ready SMS service
        run: npx esbuild src/services/cleaningReadySms.service.ts --bundle --platform=node --packages=external --outfile=/tmp/cleaningReadySms.service.js
